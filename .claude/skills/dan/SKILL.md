---
name: dan
description: |
  Alias for the `home` engine router. Mirrors Chris's total-audio-platform setup, where "/dan" also resolves to `home`. All routing logic lives in `home/SKILL.md`. This file exists so the trigger phrase "dan" / "/dan" resolves to the chrisscho.uk money-loop router.
  Trigger phrases: "dan", "/dan".
aliases: ['home', 'router']
---

# Dan (alias for `home`)

`/dan` is the muscle-memory name for the engine router. On chrisscho.uk the canonical entrypoint is
`home` — see `.claude/skills/home/SKILL.md` for the engine map, the pipeline tracker, and the
routing rules. The `dan` keyword points here and behaves identically.
